Give me the current weather info for <city>. Respond in TypeScript using this interface:

interface CityWeather {
name: string;
country: string;
coordinates: {
lat: number;
lon: number;
};
weather: {
temperatureC: number;
condition: string;
};
}

Respond only with valid TypeScript data matching this interface.
